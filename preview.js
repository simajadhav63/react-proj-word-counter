import React from 'react';

const Preview = ({ text }) => {
  return (
    <div className="preview">
      <h2>Preview</h2>
      <p>{text}</p>
    </div>
  );
};

export default Preview;
